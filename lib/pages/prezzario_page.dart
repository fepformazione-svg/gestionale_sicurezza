import 'package:flutter/material.dart';

class PrezzarioPage extends StatelessWidget {
  const PrezzarioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'PREZZARIO',
        style: TextStyle(
          fontSize: 28,
          fontWeight: FontWeight.bold,
        ),
      ),
    );
  }
}